#include<stdio.h>
int main(){
    int n ,weight,profit,i;
    printf("enter number of objects");
    scanf("%d",&n);
    int arr[n],;,
    for(i=0;i<n;i++){
        scanf("%d",&arr[i]);

    }
    printf("enter weighs of every object");
    for(i=0;i<n;i++){
        scanf("%d"arr[i]);
    }
    printf("enter profit of every object");
    for(i=0;i<n;i++){
        scanf("%d"arr[i]);
    }


    int ProfitPerKg;
    pritf("profit per kg is ");
    for(i=0;i<n;i++){
        ProfitPerKg=profit/weight;
    }
    }